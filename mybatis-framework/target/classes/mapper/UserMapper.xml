<mapper namespace="test">
    <!-- select标签，封装了SQL语句信息、入参类型、结果映射类型 -->
 <select id="queryUserByParams"
            resultType="org.example.kaikeba.mybatis.po.User"
            statementType="prepared">

        SELECT * FROM user WHERE sex = #{sex} AND username like '%${username}'
        <!-- SELECT * FROM user WHERE id = #{id} AND username like '%${username}' -->

        <!-- and sex = #{sex} AND username like '%${username}' -->
        <if test="username != null and username !='' ">
            AND username like '%${username}'
            <if test="username != null and username !=''">
                AND 1=1
            </if>
        </if>
    </select>



    <select id="queryUserByIdAndSex"
            resultType="org.example.kaikeba.mybatis.po.User"
            statementType="prepared">
        select * from user where 1 =1
        <if test="id != null" >
            and id = #{id}
        </if>
        <if test="sex != null">
            and sex = #{sex}
        </if>
    </select>

    <select id="queryUsers"
            resultType="org.example.kaikeba.mybatis.po.User"
            statementType="prepared">
        select * from user where 1 =1
    </select>
</mapper>